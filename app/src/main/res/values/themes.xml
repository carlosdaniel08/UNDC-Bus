<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UndcBusEstudiante" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/Verde</item>
        <item name="colorPrimaryDark">@color/VerdeDark</item>
        <item name="colorAccent">@color/VerdeAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.UndcBusEstudiante" parent="Base.Theme.UndcBusEstudiante" />

    <!-- Extracted repeated styles to a style resource -->
    <style name="TextStyle">
        <item name="android:fontFamily">@font/robotobold</item>
        <item name="android:textColor">@color/Black</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="AlertDialogButtonStyle">
        <!-- Cambia el color de texto de los botones -->
        <item name="android:textColor">@color/Rojo</item>
    </style>


</resources>